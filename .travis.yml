language: elixir

elixir:
  - 1.3
  - 1.4
  - 1.5

otp_release:
  - 18.3
  - 19.3
  - 20.1

install:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get --only test

addons:
    rethinkdb: '2.3'
